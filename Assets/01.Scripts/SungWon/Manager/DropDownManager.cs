using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using TMPro;

public class DropDownManager : MonoBehaviour
{

    /// 드롭다운 안씀...

    #region 드롭다운 (주석 포함)
    /* [SerializeField]
     private TMP_Dropdown dropdown;
     private string[] arrayClass = new string[2] { "메인카드", "필드카드" };

     private void Awake()
     {
         //현재 dropdown에 있는 모든 메뉴 제거
         dropdown.ClearOptions();

         //새로운 옵션 넣기 위한 OptionData 생성
         List<TMP_Dropdown.OptionData> optionlist = new List<TMP_Dropdown.OptionData>();

         //arrayClass 배열에 있는 모든 문자열 데이터를 optionList에 저장
         foreach (string str in arrayClass)
         {
             optionlist.Add(new TMP_Dropdown.OptionData(str));
         }

         // 위에서 생성한 optionList를 dropdown의 옵션 값에 추가
         dropdown.AddOptions(optionlist);

         // 현재 dropdown에 선택된 옵션을 0번으로 설정
         dropdown.value = 0;*/
    #endregion

}

